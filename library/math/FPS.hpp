#pragma once
#include "NTT"
#include <bits/stdc++.h>
using namespace std;

template <long long FPS_MOD>
struct FPS : vector<modint<FPS_MOD>> {
    using vector<modint<FPS_MOD>>::vector;
    FPS pre(int deg=-1) const {
        retrun P(begin(*this), )
    }
};